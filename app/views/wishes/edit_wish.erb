<!DOCTYPE html>
<html>
<head>
	<title>Your Christmas Wish List!!!</title>
	<meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0">
    <link href="./css/bootstrap.min.css" rel="stylesheet" media="screen">
    <link rel="stylesheet" type="text/css" href="/css/wishes.css">
</head>
<body>
	<h1>Welcome <%= @wish.user.name %> </h1>

	<h4>Edit this wish</h4>
	

	<form method="POST" action="/wishes/<%= @wish.id %>" class="text-center">
		<input type="hidden" name="_method" value="patch">	
		<input type="text" name="wish[content]" value="<%= @wish.content %>"><br>
	
        <% Event.all.each do |event| %>
            <input id="event_<%= event.id %>" type="checkbox" name="wish[event_ids][]" value="<%= event.id %>" class="text-center"<%= 'checked' if @wish.events.include?(event) %>> <%= event.event %> 
        <% end %>
        
	        <br> or create new Event:<br>
	        <input type="text" name="event" class="text-center"><br>
	        <input type="submit" name="submit" value="submit"><br>
	 </form>

	<script src="http://code.jquery.com/jquery.js"></script>
    <script src="js/bootstrap.min.js"></script>
</body>
</html>